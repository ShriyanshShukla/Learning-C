#include <stdio.h>

int main()
{
    int length = 3, breadth = 4;
    int area = length*breadth;
    printf("The area of recrangle is %d\n",area);
//  printf("The area of recrangle is %d\n", length*breadth);
    return 0;
}